# Maria Avatar Assets

Letakkan file Lottie JSON di folder ini:

- `maria-idle.json` - Animasi idle (loop 1x)
- `maria-talking.json` - Animasi bercakap (loop ∞)
- `maria-thinking.json` - Animasi berfikir (loop 2-3x)

Dan letakkan VRM model di:
- `/public/models/maria.vrm`

Avatar PNG fallback:
- `/public/maria-avatar.png`
