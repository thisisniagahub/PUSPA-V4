You are Codex CLI working in repo /mnt/g/PUSPA-V4. Complete the remaining implementation tasks from IMPLEMENTATION_PLAN_OPENCLAW_ONLY_SPEC.md Phase 5, Phase 6, and Phase 7.

Hard constraints:
- Do NOT commit or push.
- Do NOT add secrets, fake credentials, or echo env values. Supabase real env may be absent; keep build/smoke safe in headless local env.
- Preserve behavior; no broad visual redesign.
- Use TypeScript strict patterns, Next.js 16 / React 19 compatible code, import alias @/*.
- Prefer small, safe refactors. If a massive module cannot be fully split safely in one pass, create the folder convention and extract low-risk pure/config/helper/component chunks without changing behavior.
- Keep lint/typecheck/build passing.

Already done before you start:
- Z.AI provider/dependency removed.
- OpenClaw unsafe dangerouslySetInnerHTML removed.
- Users API secured.
- Supabase missing-env fallback/fail-closed middleware added.
- next.config.ts has output: standalone.
- Current explicit commands recently passed: bun run lint, bun x tsc --noEmit, bun run build.

Tasks to complete:

PHASE 5 — UI/Module Wiring/Accessibility
1) Consolidate sidebar source of truth:
   - Files: src/components/app-sidebar.tsx and src/components/sidebar/*.
   - One config should drive sidebar and command palette where feasible.
   - Role filtering must use shared access-control policy, not duplicated ad hoc role arrays.
   - Maintain/improve active visual contrast.
   - Remove duplicate active sidebar config arrays where safe.

2) Module registry consistency:
   - Files: src/types/index.ts, src/app/page.tsx, src/components/view-renderer.tsx, src/components/sidebar/sidebar-config.ts, src/components/command-palette.tsx.
   - Ensure every ViewId has a renderer decision and label/navigation metadata.
   - Decide openclaw-graph visibility: make it consistent (visible to appropriate role or explicitly hidden but registry-known).
   - Add a single module registry if feasible, for labels/search/renderer/nav metadata.
   - Add a lightweight validation/check script if practical.

3) Ctrl/Cmd+K command palette:
   - Add keyboard handler in page shell or shell hook.
   - Ctrl+K and Cmd+K open palette and prevent browser default.

4) Accessibility fixes:
   - Replace invalid div inside span in shell if present.
   - Improve active sidebar contrast.
   - Make eKYC selfie upload keyboard-operable using label/button semantics.
   - Add aria-label and type="button" to icon/password visibility buttons in settings/login/shell as needed.

PHASE 6 — Codebase Health and Modularization
1) Refactor src/modules/agihan-bulan/page.tsx into folder convention:
   src/modules/agihan-bulan/components/agihan-table.tsx
   src/modules/agihan-bulan/components/agihan-filters.tsx
   src/modules/agihan-bulan/components/agihan-dialog.tsx
   src/modules/agihan-bulan/hooks/use-agihan-data.ts
   src/modules/agihan-bulan/schemas.ts
   src/modules/agihan-bulan/types.ts
   src/modules/agihan-bulan/api.ts
   Preserve behavior and exports.

2) Repeat safe modularization for hotspots if feasible:
   - src/modules/cases/page.tsx
   - src/modules/ai/page.tsx
   - src/modules/docs/page.tsx
   - src/modules/volunteers/page.tsx
   - src/modules/members/page.tsx
   - src/modules/donations/page.tsx
   Do not do risky big-bang rewrites. Extract clear, low-risk constants/types/mock data/helper functions/components into adjacent files.

3) Race-prone number generation:
   - Review src/lib/openclaw-agent/advanced-tools.ts and similar count()+1 patterns.
   - Replace or improve with max+1 / retry / sequence helper where appropriate. Existing Gemini refactor may partially cover this; finish remaining patterns.

PHASE 7 — Validation and Release Gate
1) Run:
   - bun run lint
   - bun x tsc --noEmit
   - bun run build
2) If any fail, fix root cause and rerun.
3) Do not require real Supabase env for build. Runtime live auth can remain documented as requiring real env.
4) Write a concise report to PHASE_5_6_7_COMPLETION_REPORT.md with:
   - files changed
   - tasks completed
   - validation results
   - remaining gaps that require real credentials/manual QA only

Useful plan section lines: Phase 5 starts around line 881, Phase 6 around 969, Phase 7 around 1038 in IMPLEMENTATION_PLAN_OPENCLAW_ONLY_SPEC.md.

Begin by inspecting the relevant files, then implement, validate, and write the report.

---

---

## Current Alignment Note (2026-04-30)

This document has been aligned with the current PUSPA-V4 workspace at `/mnt/g/PUSPA-V4`:

- Stack: Next.js 16 / React 19 / TypeScript / Prisma 6 / Bun / Tailwind 4 / shadcn-Radix.
- Local dev command in `package.json` remains `bun run dev` on port `3000`; active preview work may run with `./node_modules/.bin/next dev -p 3001` when port 3000 is occupied.
- Auth: Supabase Auth is the primary app flow via `/api/v1/auth/supabase/*`, synced to Prisma users. Legacy/custom auth endpoints may remain for compatibility, but new protected API work should use server-side helpers from `@/lib/auth`.
- Route protection: `src/middleware.ts` is the active guard in this workspace. Next.js warns the middleware convention is deprecated in favor of `proxy`, so future migration should preserve the same fail-closed behavior.
- PUSPA AI/OpenClaw: Z.AI is not supported. Provider defaults should be OpenClaw-compatible, normally `openclaw/puspacare`, with env aliases for both `OPENCLAW_OPENAI_*` and `OPENCLAW_*` names. Do not commit real API keys.
- Validation baseline after the latest alignment: `bun x tsc --noEmit --pretty false` passed and `bun run build` passed.
