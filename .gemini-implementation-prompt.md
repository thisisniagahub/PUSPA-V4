You are Gemini CLI acting as the implementation agent for /mnt/g/PUSPA-V4.

Read and execute the plan at:
IMPLEMENTATION_PLAN_OPENCLAW_ONLY_SPEC.md

User request: improve the whole project and ensure OpenClaw Agent is implemented properly according to spec. Prioritize speed and safety.

Hard constraints:
1. Do not ask interactive questions. Proceed with safe defaults.
2. Do not commit or push.
3. Do not print secrets. Do not create real secrets.
4. Do not mass reformat the repo except explicit .gitattributes addition. Avoid CRLF/LF churn.
5. Do not overwrite unrelated work.
6. Remove z-ai-web-dev-sdk fully; no Z.AI fallback.
7. OpenClaw integration must be authenticated, role-gated, provider-agnostic, sanitized, audited, and safe.
8. Do not expose arbitrary shell/database mutation tools to normal app users.
9. Use minimal targeted edits. Prefer fixing critical/high risks first.
10. Write a concise progress report to GEMINI_IMPLEMENTATION_REPORT.md when done or blocked.

Execution order:
A. Inspect git status and relevant files.
B. Implement Phase 0 repo hygiene tasks that are safe: .gitattributes, .gitignore backup patterns, current-state doc. Do not delete large folders without approval; document skills ownership instead.
C. Implement Phase 1.1 remove Z.AI from package/code references. Update lock only if dependency install succeeds quickly.
D. Implement Phase 2 critical auth fixes: users API requireRole, role validation helper/policy, anonymous shell redirect, ViewRenderer access gate, access-control/sidebar/command palette sync.
E. Implement Phase 3 critical OpenClaw fixes: provider contract without Z.AI, secure chat endpoint validation/auth/role gating, tool allowlist boundary, sanitized OpenClaw message rendering. Add dependency only if needed and available; otherwise implement safe escaping fallback.
F. Implement obvious deployment/script fixes: standalone output mismatch or start script alignment, script hardcoded path fixes.
G. Run validation if dependencies are available: npm run lint, npx tsc --noEmit, npm run build. If dependencies missing, attempt one dependency install with bun install; if it hangs/fails, stop and report.
H. Write final report with: changed files, what was fixed, validation results, remaining tasks.

Important files:
- package.json, bun.lock
- next.config.ts
- .gitignore, .gitattributes
- src/app/page.tsx
- src/components/view-renderer.tsx
- src/lib/access-control.ts
- src/components/command-palette.tsx
- src/components/sidebar/sidebar-config.ts
- src/app/api/v1/users/route.ts
- src/app/api/v1/auth/supabase/login/route.ts
- src/app/api/v1/openclaw/chat/route.ts
- src/app/api/v1/openclaw/config/route.ts
- src/app/api/v1/openclaw/conversations/route.ts
- src/app/api/v1/openclaw/skills/route.ts
- src/lib/openclaw-agent/provider-types.ts
- src/lib/openclaw-agent/providers.ts
- src/lib/openclaw-agent/tools.ts
- src/lib/openclaw-agent/advanced-tools.ts
- src/lib/openclaw-agent/prompt.ts
- src/lib/openclaw-agent/memory.ts
- src/components/openclaw-agent/openclaw-message.tsx
- src/components/openclaw-agent/openclaw-message-v2.tsx

Before editing each area, read the existing file. Preserve project conventions and public APIs where possible.

---

---

## Current Alignment Note (2026-04-30)

This document has been aligned with the current PUSPA-V4 workspace at `/mnt/g/PUSPA-V4`:

- Stack: Next.js 16 / React 19 / TypeScript / Prisma 6 / Bun / Tailwind 4 / shadcn-Radix.
- Local dev command in `package.json` remains `bun run dev` on port `3000`; active preview work may run with `./node_modules/.bin/next dev -p 3001` when port 3000 is occupied.
- Auth: Supabase Auth is the primary app flow via `/api/v1/auth/supabase/*`, synced to Prisma users. Legacy/custom auth endpoints may remain for compatibility, but new protected API work should use server-side helpers from `@/lib/auth`.
- Route protection: `src/middleware.ts` is the active guard in this workspace. Next.js warns the middleware convention is deprecated in favor of `proxy`, so future migration should preserve the same fail-closed behavior.
- PUSPA AI/OpenClaw: Z.AI is not supported. Provider defaults should be OpenClaw-compatible, normally `openclaw/puspacare`, with env aliases for both `OPENCLAW_OPENAI_*` and `OPENCLAW_*` names. Do not commit real API keys.
- Validation baseline after the latest alignment: `bun x tsc --noEmit --pretty false` passed and `bun run build` passed.
